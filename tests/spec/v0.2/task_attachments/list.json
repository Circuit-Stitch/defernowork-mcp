{
  "operation": "tasks.attachments_list",
  "method": "GET",
  "path_template": "/tasks/{id}/attachments",
  "auth": "bearer",
  "request": {"headers_required": ["Authorization"]},
  "responses": [
    {
      "status": 200,
      "shape": [
        {
          "id": "uuid",
          "provider": "string",
          "filename": "string",
          "_required": ["id", "provider", "filename"]
        }
      ],
      "example": []
    }
  ],
  "client_method": "list_task_attachments",
  "client_args_from_example": [],
  "mcp_tool": "list_task_attachments",
  "mcp_tool_args_from_example": [],
  "notes": "Returns AttachmentView wire shape; s3 urls are fresh signed GETs."
}
