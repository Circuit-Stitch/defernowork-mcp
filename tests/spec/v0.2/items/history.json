{
  "operation": "items.history",
  "method": "GET",
  "path_template": "/items/{id}/history",
  "auth": "bearer",
  "request": {
    "headers_required": ["Authorization"]
  },
  "responses": [
    {
      "status": 200,
      "shape": [
        {
          "at": "datetime",
          "kind": "string",
          "_required": ["at", "kind"]
        }
      ],
      "example": [
        {"at": "2026-05-18T12:00:00Z", "kind": "Created"},
        {"at": "2026-05-18T13:30:00Z", "kind": "Edited"}
      ]
    }
  ],
  "client_method": "get_item_history",
  "client_args_from_example": [],
  "mcp_tool": "get_item_history",
  "mcp_tool_args_from_example": [],
  "notes": "Polymorphic reader — resolves item kind via the kind index, loads the entity, returns its TaskAction list. Each sibling kind (Task/Habit/Chore/Event) carries its own Vec<TaskAction>."
}
