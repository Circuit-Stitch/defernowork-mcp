{
  "operation": "habits.occurrences_reschedule",
  "method": "POST",
  "path_template": "/habits/{id}/occurrences/{date}/reschedule",
  "auth": "bearer",
  "request": {
    "headers_required": ["Authorization"],
    "body": {
      "required": ["new_date"],
      "optional": [],
      "example": {"new_date": "2026-05-17"}
    }
  },
  "responses": [
    {"status": 501, "shape": {}, "example": {}}
  ],
  "client_method": "reschedule_habit_occurrence",
  "client_args_from_example": ["new_date"],
  "mcp_tool": null,
  "mcp_tool_args_from_example": ["new_date"],
  "notes": "Backend returns 501 today (legacy habit storage). Tool exposed for forward compat -- when storage migrates this becomes 200 with the new Occurrence."
}
