{
  "operation": "items.get_by_ref",
  "method": "GET",
  "path_template": "/items/by-ref/{canonical}",
  "auth": "bearer",
  "request": {"headers_required": ["Authorization"]},
  "responses": [
    {
      "status": 200,
      "shape": {
        "kind": "string",
        "type": "string",
        "id": "uuid",
        "ref": "string",
        "org_slug": "string",
        "sequence": "number",
        "title": "string",
        "_required": ["kind", "type", "id", "org_slug"]
      },
      "example": {
        "kind": "task",
        "type": "task",
        "id": "00000000-0000-0000-0000-000000000001",
        "ref": "u-1y0e2v-123",
        "org_slug": "u-1y0e2v",
        "sequence": 123,
        "title": "Demo"
      }
    },
    {
      "status": 404,
      "error_shape": {"code": "string", "message": "string"},
      "error_example": {"code": "not_found", "message": "item not found"}
    }
  ],
  "client_method": "get_item_by_ref",
  "client_args_from_example": [],
  "mcp_tool": null,
  "mcp_tool_args_from_example": [],
  "notes": "Canonical ref resolves the org slug globally (by-ref), so it works across orgs. Wire shape is the flat ItemEnvelope carrying BOTH discriminators: inner `kind` and envelope `type`."
}
